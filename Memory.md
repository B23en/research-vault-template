# Memory

> Three sections with different lifespans. **Conventions** are standing rules —
> added only on explicit instruction, and never auto-pruned. **Open Questions**
> are long-term and are never auto-pruned either. **Working Context** is
> short-term — the agent maintains it in real time and removes items once they
> are stale or promoted into a real note.

## Conventions

<!-- Standing rules that hold across the whole vault. Added only when the user
     says so; never pruned automatically. This is the first thing an agent reads
     to know how to write here. Example:
     - Notes are written in Korean; code symbols keep their original form.
     - General domain terms and model names are defined here, not in Glossary.md,
       which is only for codes this project coins. -->

## Open Questions

<!-- Unresolved questions that shape the research. Long-lived. Example:
     - Does adaptive batching interact badly with gradient accumulation? -->

## Working Context

<!-- Short-term context the agent keeps between turns. The agent prunes this
     itself — do not expect items here to persist once they are resolved. -->
